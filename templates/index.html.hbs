<!DOCTYPE html>
<html>
<head>
    <title>Dashboard</title>
</head>
<body>
    
    {{>partials/navbar}}
   <div class="container">
    <div class="card">
        <div class="card-header text-center">
            <h1 class="m-2">{{ title_page }}</h1>
        </div>
   </div>
   <div class="container">
    <div class="row">
        <p class="text-center">This is a Server-Side {{another_field}} app.</p>
        <button class="btn btn-primary" onclick="counter()">Click Me</button>
        <p class="text-center">You have clicked the button <span id="counter">0</span> times.</p>
        <p>This is triggered with javascript</p>
    </div>
   </div>
    <script>
        function counter() {
            let countElement = document.getElementById("counter");
            let count = parseInt(countElement.innerText);
            count++;
            countElement.innerText = count;
        }
   </script>
</body>
</html>
